.header
  align-items center
  background-color $light-header
  display flex
  // justify-content space-between
  margin-bottom 2.4rem
  min-height 4.8rem
  &__container
    align-items center
    display flex
    justify-content space-between
  &__left
    align-items center
    display flex

.nav
  &__link
    font-size 1.4rem
    &:not(:last-child)
      margin-right 1em
